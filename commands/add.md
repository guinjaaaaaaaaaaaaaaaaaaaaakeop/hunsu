---
description: Record a plugin from this machine into hunsu.json (version and source)
argument-hint: <plugin>
---
!`python3 "${CLAUDE_PLUGIN_ROOT}/hunsu.py" add $ARGUMENTS --target .`

Show the output above to the user as is.
